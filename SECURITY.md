# 安全说明

## 敏感信息

请勿在 Issue、讨论、日志或截图中提交：

- 百度账号、密码、Cookie、bypy token 和一次性授权码。
- GitHub personal access token。
- SSH 密钥和打印机登录凭据。
- 完整 `printer.cfg`、内网地址、设备序列号或 MCU UUID。

## 报告漏洞

不要先公开可用于窃取凭据、任意读写文件或绕过 Moonraker 认证的细节。请通过 GitHub 的私密漏洞报告功能联系维护者。

## 安全默认值

- 默认备份根只有 `config`。
- 凭据文件权限为 `0600`。
- 不向 Fluidd 返回密码和 token。
- 上传完成前验证远端大小。
- 下载只接受历史任务 ID，不接受任意路径。
- 下载的云端目录必须通过清单与 SHA-256 验证。
- 安装器在打印进行中拒绝替换文件和重启。
